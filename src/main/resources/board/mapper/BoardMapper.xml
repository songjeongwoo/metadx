<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

  <mapper namespace="com.ktds.metadx.board.mapper.BoardMapper">
    <select id="getList" resultType="com.ktds.metadx.board.dto.BoardDTO">
        select bno, title, content, regDate, modDate, isFiles
        from board
        where bno > 0
        order by bno desc
    </select>

    <insert id="insertPost">
        insert into board (title, content, regDate, email, did, tid)
          values (#{title}, #{content}, now(), 'abcd@kt.com', 1, 1)
    </insert>
  </mapper>